pipeline {
    agent any

    stages {
        stage('Pre Build') {
            steps {
                echo 'Hello Pre Build'
            }
        }

        stage('Build') {
            steps {
                echo 'Hello Build'
            }
        }
    }
}